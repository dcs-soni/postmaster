{
  "name": "postmaster-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -p \"[{name}]\" -n \"WEB,API\" -c \"cyan.bold,green.bold\" \"bun run --filter postmaster-web dev\" \"bun run --filter postmaster-proxy-server dev\"",
    "build": "bun --filter '*' build",
    "test": "bun --filter '*' test"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
